description: Not found
content:
  application/json:
    schema:
      type: object
      required:
        - message
        - error
      properties:
        message:
          type: string
          example: NotFoundError
        error:
          type: object
          required:
            - message
          properties:
            message:
              type: string
              example: 'Contact not found'
